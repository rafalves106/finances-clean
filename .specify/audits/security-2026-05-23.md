# Auditoria de Segurança — 2026-05-23

> Por: 🔒 Security Reviewer Agent
> Escopo: pós-correção SEC-011, SEC-012 e SEC-013 + varredura de regressão/risco residual
> Constitution: 1.0.0

---

## 🎯 Veredicto Final

**[✅ APROVADO PARA MERGE]**

- Achados críticos (🔴): 0
- Achados altos (🟠): 0
- Achados médios (🟡): 0
- Achados baixos (🟢): 1

---

## 🔴 Achados Críticos (bloqueiam merge)

Nenhum.

---

## 🟠 Achados Altos

Nenhum.

---

## 🟡 Achados Médios

Nenhum.

---

## 🟢 Sugestões (não bloqueantes)

### [SEC-014] Endpoints públicos de autenticação sem rate limiting explícito

- **Arquivo**: `server/API/Controllers/AuthController.cs:17`
- **Arquivo**: `server/API/Controllers/AuthController.cs:32`
- **Arquivo**: `server/API/Program.cs:82`
- **Categoria**: Hardening de autenticação (brute-force/credential stuffing)
- **OWASP**: A07:2021 – Identification and Authentication Failures
- **Evidência**:

```text
server/API/Controllers/AuthController.cs:17:  [AllowAnonymous]
server/API/Controllers/AuthController.cs:32:  [AllowAnonymous]
```

```text
$ git grep -nE 'AddRateLimiter|UseRateLimiter|EnableRateLimiting|DisableRateLimiting' -- 'server/API/**/*.cs'
# (sem resultados)
```

- **Exploitabilidade prática**: baixa a moderada, depende de exposição pública da API e entropia real da `AdminKey`; um atacante pode automatizar tentativas no `POST /api/v1/auth/login` e `POST /api/v1/auth/registro`.
- **Impacto de negócio**: aumento de ruído operacional, risco de abuso de autenticação e eventual degradação de disponibilidade em picos maliciosos.
- **Correção sugerida (curto prazo)**:

```csharp
builder.Services.AddRateLimiter(options =>
{
    options.AddFixedWindowLimiter("auth", limiter =>
    {
        limiter.PermitLimit = 5;
        limiter.Window = TimeSpan.FromMinutes(1);
        limiter.QueueLimit = 0;
    });
});

app.UseRateLimiter();

// Em AuthController
[EnableRateLimiting("auth")]
```

- **Correção sugerida (médio prazo)**: combinar rate limit por IP + fingerprint + telemetria de tentativas para detecção de abuso.

---

## ✅ Verificações que passaram

- ✅ SEC-011 validado: separação de acesso público vs protegido está explícita.
  - `AuthController` com `[AllowAnonymous]` em login/registro (`server/API/Controllers/AuthController.cs:17`, `server/API/Controllers/AuthController.cs:32`).
  - Endpoints protegidos herdam de `AuthenticatedController` com `[Authorize]` (`server/API/Controllers/AuthenticatedController.cs:8`).
  - Pipeline de auth ativo (`app.UseAuthentication(); app.UseAuthorization();`) (`server/API/Program.cs:221`, `server/API/Program.cs:222`).
- ✅ SEC-012 validado: robustez de validação de entrada na fronteira HTTP.
  - DTOs de login/registro com validações de DataAnnotations (`server/Core/Application/DTOs/Auth/LoginDTO.cs:6`, `server/Core/Application/DTOs/Auth/RegistroDTO.cs:6`).
  - `[ApiController]` presente no controller de auth (`server/API/Controllers/AuthController.cs:9`), habilitando 400 automático com `ValidationProblemDetails` para payload inválido.
- ✅ SEC-013 validado: integridade temporal em UTC no fluxo financeiro revisado.
  - `DateTime.Now` ausente no backend (grep sem ocorrências).
  - `DateTime.UtcNow` no fluxo de remoção de investimento (`server/Core/UseCases/Investimento/RemoverInvestimentoUseCase.cs:32`).
- ✅ Sem segredos reais detectados nos padrões críticos obrigatórios (AWS/Google/OpenAI/Stripe/GitHub PAT).
- ✅ Sem padrões de SQL Injection por concatenação/interpolação nos greps obrigatórios.
- ✅ CORS endurecido com allowlist (`WithOrigins`) e sem `AllowAnyOrigin` (`server/API/Program.cs:125`).
- ✅ Headers de hardening e HTTPS presentes (`X-Frame-Options`, `Content-Security-Policy`, `UseHttpsRedirection`) (`server/API/Program.cs:154`, `server/API/Program.cs:160`, `server/API/Program.cs:170`).
- ✅ Sem algoritmos fracos para senha (nenhum match MD5/SHA1/SHA256 password).
- ✅ Dependências sem vulnerabilidades reportadas:
  - `dotnet list ... --vulnerable --include-transitive`: 0
  - `npm audit --audit-level=moderate`: 0 (high/critical/moderate)

---

## 📋 Checklist OWASP Top 10 (2021)

| #   | Risco                                      | Status |
| --- | ------------------------------------------ | ------ |
| A01 | Broken Access Control                      | ✅     |
| A02 | Cryptographic Failures                     | ✅     |
| A03 | Injection                                  | ✅     |
| A04 | Insecure Design                            | ✅     |
| A05 | Security Misconfiguration                  | ✅     |
| A06 | Vulnerable Components                      | ✅     |
| A07 | Identification and Authentication Failures | ✅     |
| A08 | Software and Data Integrity Failures       | ✅     |
| A09 | Logging and Monitoring Failures            | ✅     |
| A10 | Server-Side Request Forgery                | ✅     |

---

## 🛠️ Comandos executados (reprodutibilidade)

```bash
# Contexto obrigatório
read_file PROJECT_MAP.md
read_file .specify/memory/constitution.md
read_file .specify/audits/security-2026-05-19.md
read_file specs/001-eliminar-criticos-seguranca/spec.md

# SEC-011/012/013 (foco)
grep -RInE "\[AllowAnonymous\]|\[Authorize\]|class AuthController|class AuthenticatedController|\[ApiController\]" server/API/Controllers --include="*.cs"
grep -RInE "class (LoginDTO|RegistroDTO)|\[Required\]|\[EmailAddress\]|\[StringLength\]|\[MinLength\]|\[Range\]" server/Core/Application/DTOs server/API/Controllers --include="*.cs"
git grep -n "DateTime.Now" -- 'server/**/*.cs'
git grep -n "DateTime.UtcNow" -- 'server/**/*.cs'

# Secrets / SQLi / Auth / Hardening / Exposure / Crypto / Finance / Upload
git ls-files | grep -E '\.env$|\.env\.local|appsettings\.json|secrets\.json|credentials'
git grep -nE 'FromSqlRaw.*\$"|ExecuteSqlRaw.*\$"|query.*\+.*req\.|`SELECT.*\${|\.execute\(.*%.*\)|f"SELECT|createQuery.*\+.*request' -- 'server/**/*.cs' 'client/**/*.{js,ts}' '*.py' '*.java'
git grep -nE '\[ApiController\]|\[Authorize\]|\[AllowAnonymous\]|class AuthenticatedController|class AuthController' -- 'server/API/Controllers/*.cs' 'server/API/Controllers/**/*.cs'
git grep -nE 'AddJwtBearer|ValidateIssuerSigningKey|ValidateIssuer|ValidateAudience|ValidateLifetime|TokenValidationParameters' -- 'server/API/Program.cs'
git grep -nE 'AllowAnyOrigin|cors.*\*|origin:.*\*|X-Frame-Options|Content-Security-Policy|Strict-Transport-Security|UseHttpsRedirection|forceHttps' -- 'server/**/*.cs' 'client/**/*.{js,ts}'
git grep -nE 'console\.log.*user|logger\.(info|warn|error).*user|_logger\.Log(Information|Error).*user|ex\.StackTrace|err\.stack.*res\.|BadRequest\(ex\.Message\)|StatusCode\(500,.*ex\.Message\)|Unauthorized\(ex\.Message\)|NotFound\(ex\.Message\)|public.*Senha|public.*Password|public.*Token' -- 'server/**/*.cs' 'client/**/*.{js,ts}'
git grep -nE 'MD5|SHA1.*password|SHA256.*password' -- 'server/**/*.cs' 'client/**/*.{js,ts}'
git grep -nE 'float.*valor|double.*price|number.*amount|BeginTransaction|Database\.BeginTransaction|ITransactionManager|SaveChanges\(' -- 'server/**/*.cs' 'client/**/*.{js,ts}'
git grep -nE 'IFormFile|multipart/form-data|upload|ContentType|FileName|Path\.Combine|OpenReadStream' -- 'server/**/*.cs' 'client/**/*.{js,ts}'
git grep -nE 'AddRateLimiter|UseRateLimiter|EnableRateLimiting|DisableRateLimiting' -- 'server/API/**/*.cs'

# Dependências vulneráveis
cd server && dotnet list API/API.csproj package --vulnerable --include-transitive
cd client && npm audit --audit-level=moderate --json
```

---

## 📅 Próxima auditoria recomendada

Antes do próximo release que exponha novos endpoints públicos de autenticação/autorização, ou no máximo em 30 dias.
